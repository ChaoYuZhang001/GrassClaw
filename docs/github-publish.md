# Publish to GitHub

```bash
git init
git add .
git commit -m "feat: release-ready GrassClaw repo"
git branch -M main
git remote add origin https://github.com/<your-account>/GrassClaw.git
git push -u origin main
```
